import 'package:flutter/material.dart';

class HotelSectionViewAll extends StatelessWidget {
  const HotelSectionViewAll({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('All Hotels'),
        centerTitle: true,
      ),
    );
  }
} 